[pytest]
testpaths = tests
markers =
    fortran: tests that compile or inspect Fortran code
    orblib_fortran: tests that require orblib Fortran executables or outputs
    slow: integration tests that can take minutes
    cvxopt: tests that require the optional cvxopt package
    galahad: tests that require GALAHAD-linked legacy executables
addopts = -ra
